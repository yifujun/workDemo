<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metlife.wechat.modules.dao.adconf.AdConfDao">

    <select id="getAdConfs" parameterType="string" resultType="com.metlife.wechat.modules.model.adconf.AdConfPO">
        SELECT 
            ImageUrl imageUrl,
            ImageTitle imageTitle,
            RedirectTo redirectTo
        FROM
            ml_AdConf
        WHERE
            ConfType = #{ confType }
        AND AdTypeTag = #{ adTypeTag }
        AND IsDeleted = 0
        AND IsShow = 1
        ORDER BY
            orderNo ASC
    </select>
</mapper>
